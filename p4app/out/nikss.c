/* Automatically generated by p4c-ebpf from src/nikss.p4 on Fri Feb 23 14:05:08 2024
 */
#include "ebpf_kernel.h"

#include <stdbool.h>
#include <linux/if_ether.h>
#include "psa.h"

#define EBPF_MASK(t, w) ((((t)(1)) << (w)) - (t)1)
#define BYTES(w) ((w) / 8)
#define write_partial(a, w, s, v) do { *((u8*)a) = ((*((u8*)a)) & ~(EBPF_MASK(u8, w) << s)) | (v << s) ; } while (0)
#define write_byte(base, offset, v) do { *(u8*)((base) + (offset)) = (v); } while (0)
#define bpf_trace_message(fmt, ...)


#define CLONE_MAX_PORTS 64
#define CLONE_MAX_INSTANCES 1
#define CLONE_MAX_CLONES (CLONE_MAX_PORTS * CLONE_MAX_INSTANCES)
#define CLONE_MAX_SESSIONS 1024

#ifndef PSA_PORT_RECIRCULATE
#define PSA_PORT_RECIRCULATE 0
#endif
#define P4C_PSA_PORT_RECIRCULATE 0xfffffffa

struct internal_metadata {
    __u16 pkt_ether_type;
} __attribute__((aligned(4)));

struct list_key_t {
    __u32 port;
    __u16 instance;
};
typedef struct list_key_t elem_t;

struct element {
    struct clone_session_entry entry;
    elem_t next_id;
} __attribute__((aligned(4)));


struct metadata {
    u32 probability; /* bit<32> */
    u32 pr1; /* bit<32> */
    u32 pr2; /* bit<32> */
    u16 flow_id; /* bit<16> */
    u32 out_if; /* bit<32> */
};
struct empty_t {
};
struct ethernet_t {
    u64 dstMac; /* bit<48> */
    u64 srcMac; /* bit<48> */
    u16 etherType; /* bit<16> */
    u8 ebpf_valid;
};
struct ipv4_t {
    u8 version; /* bit<4> */
    u8 ihl; /* bit<4> */
    u8 diffserv; /* bit<8> */
    u16 totallen; /* bit<16> */
    u16 identification; /* bit<16> */
    u8 flags; /* bit<3> */
    u16 fragOffset; /* bit<13> */
    u8 ttl; /* bit<8> */
    u8 protocol; /* bit<8> */
    u16 hdrChecksum; /* bit<16> */
    u32 srcIP; /* bit<32> */
    u32 dstIP; /* bit<32> */
    u8 ebpf_valid;
};
struct udp_t {
    u16 srcPort; /* bit<16> */
    u16 dstPort; /* bit<16> */
    u16 length; /* bit<16> */
    u16 checksum; /* bit<16> */
    u8 ebpf_valid;
};
struct int_md_t {
    u16 flow_id; /* bit<16> */
    u64 delay; /* bit<64> */
    u64 jitter; /* bit<64> */
    u8 ebpf_valid;
};
struct headers {
    struct ethernet_t ethernet; /* ethernet_t */
    struct ipv4_t ipv4; /* ipv4_t */
    struct udp_t udp; /* udp_t */
    struct int_md_t int_md; /* int_md_t */
};
struct tuple_0 {
    u8 f0; /* bit<4> */
    u8 f1; /* bit<4> */
    u8 f2; /* bit<8> */
    u16 f3; /* bit<16> */
    u16 f4; /* bit<16> */
    u8 f5; /* bit<3> */
    u16 f6; /* bit<13> */
    u8 f7; /* bit<8> */
    u8 f8; /* bit<8> */
    u32 f9; /* bit<32> */
    u32 f10; /* bit<32> */
};
struct tuple_1 {
    u16 f0; /* bit<16> */
};
struct tuple_2 {
    u16 f0; /* bit<16> */
    u64 f1; /* bit<64> */
    u64 f2; /* bit<64> */
};
struct ingress_tbl_fwd1_key {
    u32 prefixlen;
    u32 field0; /* istd.ingress_port */
    u32 field1; /* meta.out_if */
    u32 field2; /* hdr.ipv4.dstIP */
} __attribute__((aligned(4)));
#define INGRESS_TBL_FWD1_ACT_INGRESS_FWD_INT 1
#define INGRESS_TBL_FWD1_ACT_INGRESS_FWD_2ND_PASS 2
struct ingress_tbl_fwd1_value {
    unsigned int action;
    union {
        struct {
        } _NoAction;
        struct {
            u32 eif;
            u64 dmac;
            u64 added_delay;
        } ingress_fwd_int;
        struct {
            u32 eif;
            u64 colMAC;
        } ingress_fwd_2nd_pass;
    } u;
};
struct ingress_tbl_params_key {
    u32 prefixlen;
    u32 field0; /* istd.ingress_port */
    u32 field1; /* hdr.ipv4.dstIP */
} __attribute__((aligned(4)));
#define INGRESS_TBL_PARAMS_ACT_INGRESS_SET_PARAMS 1
struct ingress_tbl_params_value {
    unsigned int action;
    union {
        struct {
        } _NoAction;
        struct {
            u32 pr1;
            u32 pr2;
            u16 flow_id;
        } ingress_set_params;
    } u;
    struct {
        u32 bytes;
        u32 packets;
    } ingress_counter_int;
};
typedef u16 ingress_reg_last_arrival_key;
typedef u64 ingress_reg_last_arrival_value;
struct egress_tbl_int_key {
    u32 field0; /* istd.egress_port */
} __attribute__((aligned(4)));
#define EGRESS_TBL_INT_ACT_EGRESS_INSERT_MD 1
#define EGRESS_TBL_INT_ACT_EGRESS_JUST_CLONE 2
struct egress_tbl_int_value {
    unsigned int action;
    union {
        struct {
        } _NoAction;
        struct {
        } egress_insert_md;
        struct {
        } egress_just_clone;
    } u;
};

struct lookup_tbl_val {
    u32 table[2048];
};

struct hdr_md {
    struct headers cpumap_hdr;
    struct metadata cpumap_usermeta;
    __u8 __hook;
};

REGISTER_START()
REGISTER_TABLE_INNER(clone_session_tbl_inner, BPF_MAP_TYPE_HASH, elem_t, struct element, 64, 1, 1)
BPF_ANNOTATE_KV_PAIR(clone_session_tbl_inner, elem_t, struct element)
REGISTER_TABLE_OUTER(clone_session_tbl, BPF_MAP_TYPE_ARRAY_OF_MAPS, __u32, __u32, 1024, 1, clone_session_tbl_inner)
BPF_ANNOTATE_KV_PAIR(clone_session_tbl, __u32, __u32)
REGISTER_TABLE_INNER(multicast_grp_tbl_inner, BPF_MAP_TYPE_HASH, elem_t, struct element, 64, 2, 2)
BPF_ANNOTATE_KV_PAIR(multicast_grp_tbl_inner, elem_t, struct element)
REGISTER_TABLE_OUTER(multicast_grp_tbl, BPF_MAP_TYPE_ARRAY_OF_MAPS, __u32, __u32, 1024, 2, multicast_grp_tbl_inner)
BPF_ANNOTATE_KV_PAIR(multicast_grp_tbl, __u32, __u32)
REGISTER_TABLE_FLAGS(ingress_tbl_fwd1, BPF_MAP_TYPE_LPM_TRIE, struct ingress_tbl_fwd1_key, struct ingress_tbl_fwd1_value, 1024, BPF_F_NO_PREALLOC)
BPF_ANNOTATE_KV_PAIR(ingress_tbl_fwd1, struct ingress_tbl_fwd1_key, struct ingress_tbl_fwd1_value)
REGISTER_TABLE(ingress_tbl_fwd1_defaultAction, BPF_MAP_TYPE_ARRAY, u32, struct ingress_tbl_fwd1_value, 1)
BPF_ANNOTATE_KV_PAIR(ingress_tbl_fwd1_defaultAction, u32, struct ingress_tbl_fwd1_value)
REGISTER_TABLE_FLAGS(ingress_tbl_params, BPF_MAP_TYPE_LPM_TRIE, struct ingress_tbl_params_key, struct ingress_tbl_params_value, 1024, BPF_F_NO_PREALLOC)
BPF_ANNOTATE_KV_PAIR(ingress_tbl_params, struct ingress_tbl_params_key, struct ingress_tbl_params_value)
REGISTER_TABLE(ingress_tbl_params_defaultAction, BPF_MAP_TYPE_ARRAY, u32, struct ingress_tbl_params_value, 1)
BPF_ANNOTATE_KV_PAIR(ingress_tbl_params_defaultAction, u32, struct ingress_tbl_params_value)
REGISTER_TABLE(ingress_reg_last_arrival, BPF_MAP_TYPE_ARRAY, u32, ingress_reg_last_arrival_value, 64)
BPF_ANNOTATE_KV_PAIR(ingress_reg_last_arrival, u32, ingress_reg_last_arrival_value)
REGISTER_TABLE(egress_tbl_int, BPF_MAP_TYPE_HASH, struct egress_tbl_int_key, struct egress_tbl_int_value, 1024)
BPF_ANNOTATE_KV_PAIR(egress_tbl_int, struct egress_tbl_int_key, struct egress_tbl_int_value)
REGISTER_TABLE(egress_tbl_int_defaultAction, BPF_MAP_TYPE_ARRAY, u32, struct egress_tbl_int_value, 1)
BPF_ANNOTATE_KV_PAIR(egress_tbl_int_defaultAction, u32, struct egress_tbl_int_value)
REGISTER_TABLE(hdr_md_cpumap, BPF_MAP_TYPE_PERCPU_ARRAY, u32, struct hdr_md, 2)
BPF_ANNOTATE_KV_PAIR(hdr_md_cpumap, u32, struct hdr_md)
REGISTER_TABLE(crc_lookup_tbl, BPF_MAP_TYPE_ARRAY, u32, struct lookup_tbl_val, 1)
BPF_ANNOTATE_KV_PAIR(crc_lookup_tbl, u32, struct lookup_tbl_val)
REGISTER_END()

static __always_inline
void crc16_update(u16 * reg, const u8 * data, u16 data_size, const u16 poly) {
    if (data_size <= 8)
        data += data_size - 1;
    #pragma clang loop unroll(full)
    for (u16 i = 0; i < data_size; i++) {
        bpf_trace_message("CRC16: data byte: %x\n", *data);
        *reg ^= *data;
        for (u8 bit = 0; bit < 8; bit++) {
            *reg = (*reg) & 1 ? ((*reg) >> 1) ^ poly : (*reg) >> 1;
        }
        if (data_size <= 8)
            data--;
        else
            data++;
    }
}
static __always_inline u16 crc16_finalize(u16 reg) {
    return reg;
}
static __always_inline
void crc32_update(u32 * reg, const u8 * data, u16 data_size, const u32 poly) {
    u32* current = (u32*) data;
    struct lookup_tbl_val* lookup_table;
    u32 index = 0;
    lookup_table = BPF_MAP_LOOKUP_ELEM(crc_lookup_tbl, &index);
    u32 lookup_key = 0;
    u32 lookup_value = 0;
    u32 lookup_value1 = 0;
    u32 lookup_value2 = 0;
    u32 lookup_value3 = 0;
    u32 lookup_value4 = 0;
    u32 lookup_value5 = 0;
    u32 lookup_value6 = 0;
    u32 lookup_value7 = 0;
    u32 lookup_value8 = 0;
    u16 tmp = 0;
    if (lookup_table != NULL) {
        for (u16 i = data_size; i >= 8; i -= 8) {
            /* Vars one and two will have swapped byte order if data_size == 8 */
            if (data_size == 8) current = data + 4;
            bpf_trace_message("CRC32: data dword: %x\n", *current);
            u32 one = (data_size == 8 ? __builtin_bswap32(*current--) : *current++) ^ *reg;
            bpf_trace_message("CRC32: data dword: %x\n", *current);
            u32 two = (data_size == 8 ? __builtin_bswap32(*current--) : *current++);
            lookup_key = (one & 0x000000FF);
            lookup_value8 = lookup_table->table[(u16)(1792 + (u8)lookup_key)];
            lookup_key = (one >> 8) & 0x000000FF;
            lookup_value7 = lookup_table->table[(u16)(1536 + (u8)lookup_key)];
            lookup_key = (one >> 16) & 0x000000FF;
            lookup_value6 = lookup_table->table[(u16)(1280 + (u8)lookup_key)];
            lookup_key = one >> 24;
            lookup_value5 = lookup_table->table[(u16)(1024 + (u8)(lookup_key))];
            lookup_key = (two & 0x000000FF);
            lookup_value4 = lookup_table->table[(u16)(768 + (u8)lookup_key)];
            lookup_key = (two >> 8) & 0x000000FF;
            lookup_value3 = lookup_table->table[(u16)(512 + (u8)lookup_key)];
            lookup_key = (two >> 16) & 0x000000FF;
            lookup_value2 = lookup_table->table[(u16)(256 + (u8)lookup_key)];
            lookup_key = two >> 24;
            lookup_value1 = lookup_table->table[(u8)(lookup_key)];
            *reg = lookup_value8 ^ lookup_value7 ^ lookup_value6 ^ lookup_value5 ^
                   lookup_value4 ^ lookup_value3 ^ lookup_value2 ^ lookup_value1;
            tmp += 8;
        }
        volatile int std_algo_lookup_key = 0;
        if (data_size < 8) {
            unsigned char *currentChar = (unsigned char *) current;
            currentChar += data_size - 1;
            for (u16 i = tmp; i < data_size; i++) {
                bpf_trace_message("CRC32: data byte: %x\n", *currentChar);
                std_algo_lookup_key = (u32)(((*reg) & 0xFF) ^ *currentChar--);
                if (std_algo_lookup_key >= 0) {
                    lookup_value = lookup_table->table[(u8)(std_algo_lookup_key & 255)];
                }
                *reg = ((*reg) >> 8) ^ lookup_value;
            }
        } else {
            /* Consume data not processed by slice-by-8 algorithm above, these data are in network byte order */
            unsigned char *currentChar = (unsigned char *) current;
            for (u16 i = tmp; i < data_size; i++) {
                bpf_trace_message("CRC32: data byte: %x\n", *currentChar);
                std_algo_lookup_key = (u32)(((*reg) & 0xFF) ^ *currentChar++);
                if (std_algo_lookup_key >= 0) {
                    lookup_value = lookup_table->table[(u8)(std_algo_lookup_key & 255)];
                }
                *reg = ((*reg) >> 8) ^ lookup_value;
            }
        }
    }
}
static __always_inline u32 crc32_finalize(u32 reg) {
    return reg ^ 0xFFFFFFFF;
}
inline u16 csum16_add(u16 csum, u16 addend) {
    u16 res = csum;
    res += addend;
    return (res + (res < addend));
}
inline u16 csum16_sub(u16 csum, u16 addend) {
    return csum16_add(csum, ~addend);
}
static __always_inline
int do_for_each(SK_BUFF *skb, void *map, unsigned int max_iter, void (*a)(SK_BUFF *, void *))
{
    elem_t head_idx = {0, 0};
    struct element *elem = bpf_map_lookup_elem(map, &head_idx);
    if (!elem) {
        return -1;
    }
    if (elem->next_id.port == 0 && elem->next_id.instance == 0) {
               return 0;
    }
    elem_t next_id = elem->next_id;
    for (unsigned int i = 0; i < max_iter; i++) {
        struct element *elem = bpf_map_lookup_elem(map, &next_id);
        if (!elem) {
            break;
        }
        a(skb, &elem->entry);
        if (elem->next_id.port == 0 && elem->next_id.instance == 0) {
            break;
        }
        next_id = elem->next_id;
    }
    return 0;
}

static __always_inline
void do_clone(SK_BUFF *skb, void *data)
{
    struct clone_session_entry *entry = (struct clone_session_entry *) data;
    bpf_clone_redirect(skb, entry->egress_port, 0);
}

static __always_inline
int do_packet_clones(SK_BUFF * skb, void * map, __u32 session_id, PSA_PacketPath_t new_pkt_path, __u8 caller_id)
{
    struct psa_global_metadata * meta = (struct psa_global_metadata *) skb->cb;
    void * inner_map;
    inner_map = bpf_map_lookup_elem(map, &session_id);
    if (inner_map != NULL) {
        PSA_PacketPath_t original_pkt_path = meta->packet_path;
        meta->packet_path = new_pkt_path;
        if (do_for_each(skb, inner_map, CLONE_MAX_CLONES, &do_clone) < 0) {
            return -1;
        }
        meta->packet_path = original_pkt_path;
    } else {
    }
    return 0;
 }

static __always_inline
void add_prefix_and_entries(__u32 nr_entries,
            __u32 tuple_id,
            void *tuples_map,
            void *prefixes_map,
            void *key_mask,
            void *value_mask,
            void *keysPtrs[],
            void *valuesPtrs[]) {
    int ret = bpf_map_update_elem(prefixes_map, key_mask, value_mask, BPF_ANY);
    if (ret) {
        return;
    }
    if (nr_entries == 0) {
        return;
    }
    struct bpf_elf_map *tuple = bpf_map_lookup_elem(tuples_map, &tuple_id);
    if (tuple) {
        for (__u32 i = 0; i < nr_entries; i++) {
            int ret = bpf_map_update_elem(tuple, keysPtrs[i], valuesPtrs[i], BPF_ANY);
            if (ret) {
                return;
            } else {
            }
        }
    } else {
        return;
    }
}

SEC("classifier/map-initializer")
int map_initializer() {
    u32 ebpf_zero = 0;

    u32 lookup_tbl_key = 0;
    struct lookup_tbl_val* lookup_tbl_value = BPF_MAP_LOOKUP_ELEM(crc_lookup_tbl, &lookup_tbl_key);
    if (lookup_tbl_value != NULL){
        for (u16 i = 0; i <= 255; i++){
            u32 crc = i;
            for (u16 j = 0; j < 8; j++){
                crc = (crc >> 1) ^ ((crc & 1) * 3988292384);
            }
            lookup_tbl_value->table[i] = crc;
        }
        for (u16 i = 0; i <= 255; i++){
            lookup_tbl_value->table[256+i] = (lookup_tbl_value->table[0+i] >> 8) ^ lookup_tbl_value->table[(lookup_tbl_value->table[0+i] & 0xFF)];
            lookup_tbl_value->table[512+i] = (lookup_tbl_value->table[256+i] >> 8) ^ lookup_tbl_value->table[(lookup_tbl_value->table[256+i] & 0xFF)];
            lookup_tbl_value->table[768+i] = (lookup_tbl_value->table[512+i] >> 8) ^ lookup_tbl_value->table[(lookup_tbl_value->table[512+i] & 0xFF)];
            lookup_tbl_value->table[1024+i] = (lookup_tbl_value->table[768+i] >> 8) ^ lookup_tbl_value->table[(lookup_tbl_value->table[768+i] & 0xFF)];
            lookup_tbl_value->table[1280+i] = (lookup_tbl_value->table[1024+i] >> 8) ^ lookup_tbl_value->table[(lookup_tbl_value->table[1024+i] & 0xFF)];
            lookup_tbl_value->table[1536+i] = (lookup_tbl_value->table[1280+i] >> 8) ^ lookup_tbl_value->table[(lookup_tbl_value->table[1280+i] & 0xFF)];
            lookup_tbl_value->table[1792+i] = (lookup_tbl_value->table[1536+i] >> 8) ^ lookup_tbl_value->table[(lookup_tbl_value->table[1536+i] & 0xFF)];
        }
    }
    return 0;
}

SEC("xdp/xdp-ingress")
int xdp_func(struct xdp_md *skb) {
        void *data_end = (void *)(long)skb->data_end;
    struct ethhdr *eth = (struct ethhdr *)(long)skb->data;
    if ((void *)((struct ethhdr *) eth + 1) > data_end) {
        return XDP_ABORTED;
    }
    if (eth->h_proto == bpf_htons(0x0800) || eth->h_proto == bpf_htons(0x86DD)) {
        return XDP_PASS;
    }

    struct internal_metadata *meta;
    int ret = bpf_xdp_adjust_meta(skb, -(int)sizeof(*meta));
    if (ret < 0) {
        return XDP_ABORTED;
    }
    meta = (struct internal_metadata *)(unsigned long)skb->data_meta;
    eth = (void *)(long)skb->data;
    data_end = (void *)(long)skb->data_end;
    if ((void *) ((struct internal_metadata *) meta + 1) > (void *)(long)skb->data)
        return XDP_ABORTED;
    if ((void *)((struct ethhdr *) eth + 1) > data_end) {
        return XDP_ABORTED;
    }
    meta->pkt_ether_type = eth->h_proto;
    eth->h_proto = bpf_htons(0x0800);

    return XDP_PASS;
}
static __always_inline int process(struct __sk_buff *skb, struct headers *parsed_hdr, struct psa_ingress_output_metadata_t *ostd, struct psa_global_metadata *compiler_meta__, struct empty_t *resubmit_meta)
{
    unsigned ebpf_packetOffsetInBits = 0;
    unsigned ebpf_packetOffsetInBits_save = 0;
    ParserError_t ebpf_errorCode = NoError;
    void* pkt = ((void*)(long)skb->data);
    void* ebpf_packetEnd = ((void*)(long)skb->data_end);
    u32 ebpf_zero = 0;
    u32 ebpf_one = 1;
    unsigned char ebpf_byte;
    u32 pkt_len = skb->len;
    u64 tstamp = bpf_ktime_get_ns();
    u32 ebpf_input_port = skb->ifindex;
    if (ebpf_input_port == PSA_PORT_RECIRCULATE) {
        ebpf_input_port = P4C_PSA_PORT_RECIRCULATE;
    }

    struct metadata *meta;
    struct hdr_md *hdrMd;

    hdrMd = BPF_MAP_LOOKUP_ELEM(hdr_md_cpumap, &ebpf_zero);
    if (!hdrMd)
        return TC_ACT_SHOT;
    __builtin_memset(hdrMd, 0, sizeof(struct hdr_md));

    parsed_hdr = &(hdrMd->cpumap_hdr);
    meta = &(hdrMd->cpumap_usermeta);
    {
        struct psa_ingress_parser_input_metadata_t istd = {
            .ingress_port = ebpf_input_port,
            .packet_path = compiler_meta__->packet_path,
        };
        goto start;
        parse_int: {
/* extract(parsed_hdr->int_md) */
            if (ebpf_packetEnd < pkt + BYTES(ebpf_packetOffsetInBits + 144 + 0)) {
                ebpf_errorCode = PacketTooShort;
                goto reject;
            }

            parsed_hdr->int_md.flow_id = (u16)((load_half(pkt, BYTES(ebpf_packetOffsetInBits))));
            ebpf_packetOffsetInBits += 16;

            parsed_hdr->int_md.delay = (u64)((load_dword(pkt, BYTES(ebpf_packetOffsetInBits))));
            ebpf_packetOffsetInBits += 64;

            parsed_hdr->int_md.jitter = (u64)((load_dword(pkt, BYTES(ebpf_packetOffsetInBits))));
            ebpf_packetOffsetInBits += 64;

            parsed_hdr->int_md.ebpf_valid = 1;

;
             goto accept;
        }
        parse_ipv4: {
/* extract(parsed_hdr->ipv4) */
            if (ebpf_packetEnd < pkt + BYTES(ebpf_packetOffsetInBits + 160 + 0)) {
                ebpf_errorCode = PacketTooShort;
                goto reject;
            }

            parsed_hdr->ipv4.version = (u8)((load_byte(pkt, BYTES(ebpf_packetOffsetInBits)) >> 4) & EBPF_MASK(u8, 4));
            ebpf_packetOffsetInBits += 4;

            parsed_hdr->ipv4.ihl = (u8)((load_byte(pkt, BYTES(ebpf_packetOffsetInBits))) & EBPF_MASK(u8, 4));
            ebpf_packetOffsetInBits += 4;

            parsed_hdr->ipv4.diffserv = (u8)((load_byte(pkt, BYTES(ebpf_packetOffsetInBits))));
            ebpf_packetOffsetInBits += 8;

            parsed_hdr->ipv4.totallen = (u16)((load_half(pkt, BYTES(ebpf_packetOffsetInBits))));
            ebpf_packetOffsetInBits += 16;

            parsed_hdr->ipv4.identification = (u16)((load_half(pkt, BYTES(ebpf_packetOffsetInBits))));
            ebpf_packetOffsetInBits += 16;

            parsed_hdr->ipv4.flags = (u8)((load_byte(pkt, BYTES(ebpf_packetOffsetInBits)) >> 5) & EBPF_MASK(u8, 3));
            ebpf_packetOffsetInBits += 3;

            parsed_hdr->ipv4.fragOffset = (u16)((load_half(pkt, BYTES(ebpf_packetOffsetInBits))) & EBPF_MASK(u16, 13));
            ebpf_packetOffsetInBits += 13;

            parsed_hdr->ipv4.ttl = (u8)((load_byte(pkt, BYTES(ebpf_packetOffsetInBits))));
            ebpf_packetOffsetInBits += 8;

            parsed_hdr->ipv4.protocol = (u8)((load_byte(pkt, BYTES(ebpf_packetOffsetInBits))));
            ebpf_packetOffsetInBits += 8;

            parsed_hdr->ipv4.hdrChecksum = (u16)((load_half(pkt, BYTES(ebpf_packetOffsetInBits))));
            ebpf_packetOffsetInBits += 16;

            parsed_hdr->ipv4.srcIP = (u32)((load_word(pkt, BYTES(ebpf_packetOffsetInBits))));
            ebpf_packetOffsetInBits += 32;

            parsed_hdr->ipv4.dstIP = (u32)((load_word(pkt, BYTES(ebpf_packetOffsetInBits))));
            ebpf_packetOffsetInBits += 32;

            parsed_hdr->ipv4.ebpf_valid = 1;

;
            u8 select_0;
            select_0 = parsed_hdr->ipv4.protocol;
            if (select_0 == 17)goto parse_udp;
            if ((select_0 & 0x0) == (0x0 & 0x0))goto accept;
            else goto reject;
        }
        parse_udp: {
/* extract(parsed_hdr->udp) */
            if (ebpf_packetEnd < pkt + BYTES(ebpf_packetOffsetInBits + 64 + 0)) {
                ebpf_errorCode = PacketTooShort;
                goto reject;
            }

            parsed_hdr->udp.srcPort = (u16)((load_half(pkt, BYTES(ebpf_packetOffsetInBits))));
            ebpf_packetOffsetInBits += 16;

            parsed_hdr->udp.dstPort = (u16)((load_half(pkt, BYTES(ebpf_packetOffsetInBits))));
            ebpf_packetOffsetInBits += 16;

            parsed_hdr->udp.length = (u16)((load_half(pkt, BYTES(ebpf_packetOffsetInBits))));
            ebpf_packetOffsetInBits += 16;

            parsed_hdr->udp.checksum = (u16)((load_half(pkt, BYTES(ebpf_packetOffsetInBits))));
            ebpf_packetOffsetInBits += 16;

            parsed_hdr->udp.ebpf_valid = 1;

;
            u8 select_1;
            select_1 = parsed_hdr->ipv4.diffserv;
            if (select_1 == 32)goto parse_int;
            if ((select_1 & 0x0) == (0x0 & 0x0))goto accept;
            else goto reject;
        }
        start: {
/* extract(parsed_hdr->ethernet) */
            if (ebpf_packetEnd < pkt + BYTES(ebpf_packetOffsetInBits + 112 + 0)) {
                ebpf_errorCode = PacketTooShort;
                goto reject;
            }

            parsed_hdr->ethernet.dstMac = (u64)((load_dword(pkt, BYTES(ebpf_packetOffsetInBits)) >> 16) & EBPF_MASK(u64, 48));
            ebpf_packetOffsetInBits += 48;

            parsed_hdr->ethernet.srcMac = (u64)((load_dword(pkt, BYTES(ebpf_packetOffsetInBits)) >> 16) & EBPF_MASK(u64, 48));
            ebpf_packetOffsetInBits += 48;

            parsed_hdr->ethernet.etherType = (u16)((load_half(pkt, BYTES(ebpf_packetOffsetInBits))));
            ebpf_packetOffsetInBits += 16;

            parsed_hdr->ethernet.ebpf_valid = 1;

;
            u16 select_2;
            select_2 = parsed_hdr->ethernet.etherType;
            if (select_2 == 2048)goto parse_ipv4;
            if ((select_2 & 0x0) == (0x0 & 0x0))goto accept;
            else goto reject;
        }

        reject: {
            if (ebpf_errorCode == 0) {
                return TC_ACT_SHOT;
            }
            goto accept;
        }

    }

    accept: {
        struct psa_ingress_input_metadata_t istd = {
            .ingress_port = ebpf_input_port,
            .packet_path = compiler_meta__->packet_path,
            .parser_error = ebpf_errorCode,
    };
        istd.ingress_timestamp = tstamp;
        u8 hit;
        u64 delay_tmp_0 = 0;
        u64 tmp = 0;
        u32 tmp_0 = 0;
        u64 tmp_1 = 0;
        u64 tmp_2 = 0;
        u64 tmp_3 = 0;
        u64 tmp_4 = 0;
        u64 tmp_5 = 0;
        struct psa_ingress_output_metadata_t *meta_1;
        u32 egress_port_1;
        struct psa_ingress_output_metadata_t *meta_2;
        u32 egress_port_2;
        {
if (/* parsed_hdr->ipv4.isValid() */
            parsed_hdr->ipv4.ebpf_valid) {
/* tbl_params_0.apply() */
                {
                    /* construct key */
                    struct ingress_tbl_params_key key = {};
                    key.prefixlen = sizeof(key)*8 - 32;
                    key.field0 = istd.ingress_port;
                    key.field1 = bpf_htonl(parsed_hdr->ipv4.dstIP);
                    /* value */
                    struct ingress_tbl_params_value *value = NULL;
                    /* perform lookup */
                    value = BPF_MAP_LOOKUP_ELEM(ingress_tbl_params, &key);
                    if (value == NULL) {
                        /* miss; find default action */
                        hit = 0;
                        value = BPF_MAP_LOOKUP_ELEM(ingress_tbl_params_defaultAction, &ebpf_zero);
                    } else {
                        hit = 1;
                    }
                    if (value != NULL) {
                        /* run action */
                        switch (value->action) {
                            case 0: 
                                {
                                }
                                break;
                            case INGRESS_TBL_PARAMS_ACT_INGRESS_SET_PARAMS: 
                                {
/* counter_int_0.count() */
                                    __sync_fetch_and_add(&(value->ingress_counter_int.bytes), pkt_len);
                                    __sync_fetch_and_add(&(value->ingress_counter_int.packets), 1);
;
                                                                        meta->flow_id = value->u.ingress_set_params.flow_id;
                                                                        meta->probability = /* probability_0.read() */
(bpf_get_prandom_u32() & 0x3ffu);
                                                                        meta->pr1 = value->u.ingress_set_params.pr1;
                                                                        meta->pr2 = value->u.ingress_set_params.pr2;
                                }
                                break;
                            default:
                                return TC_ACT_SHOT;
                        }
                    } else {
                        return TC_ACT_SHOT;
;
                    }
                }
                if (hit) {
if (meta->probability < meta->pr1) {
                        meta->flow_id = (meta->flow_id + 1);
                        if (meta->probability < (meta->pr1 >> 1)) {
                            meta->out_if = 10;                        }

                        else {
                            meta->out_if = 11;                        }

                    }
                    else {
if (meta->probability < meta->pr2) {
                            meta->out_if = 20;
                                                        meta->flow_id = (meta->flow_id + 2);
                        }
                        else {
                            meta->out_if = 30;
                                                        meta->flow_id = (meta->flow_id + 3);
                        }                    }
                }

                else {
                    meta->out_if = 0;                }

                /* tbl_fwd1_0.apply() */
                {
                    /* construct key */
                    struct ingress_tbl_fwd1_key key = {};
                    key.prefixlen = sizeof(key)*8 - 32;
                    key.field0 = istd.ingress_port;
                    key.field1 = meta->out_if;
                    key.field2 = bpf_htonl(parsed_hdr->ipv4.dstIP);
                    /* value */
                    struct ingress_tbl_fwd1_value *value = NULL;
                    /* perform lookup */
                    value = BPF_MAP_LOOKUP_ELEM(ingress_tbl_fwd1, &key);
                    if (value == NULL) {
                        /* miss; find default action */
                        hit = 0;
                        value = BPF_MAP_LOOKUP_ELEM(ingress_tbl_fwd1_defaultAction, &ebpf_zero);
                    } else {
                        hit = 1;
                    }
                    if (value != NULL) {
                        /* run action */
                        switch (value->action) {
                            case 0: 
                                {
                                }
                                break;
                            case INGRESS_TBL_FWD1_ACT_INGRESS_FWD_INT: 
                                {
                                    meta_1 = ostd;
                                                                        egress_port_1 = value->u.ingress_fwd_int.eif;
                                                                        meta_1->drop = false;
                                                                        meta_1->multicast_group = 0;
                                                                        meta_1->egress_port = egress_port_1;
                                                                        ostd = meta_1;
                                                                        parsed_hdr->ipv4.ttl = (parsed_hdr->ipv4.ttl + 255);
                                                                        parsed_hdr->ethernet.dstMac = value->u.ingress_fwd_int.dmac;
                                    /* parsed_hdr->int_md.setValid() */
                                    parsed_hdr->int_md.ebpf_valid = true;
                                                                        parsed_hdr->int_md.flow_id = meta->flow_id;
                                                                        delay_tmp_0 = (u64)istd.ingress_timestamp;
                                                                        tmp = (delay_tmp_0 - value->u.ingress_fwd_int.added_delay);
                                                                        tmp_0 = /* probability_0.read() */
(bpf_get_prandom_u32() & 0x3ffu);
                                                                        tmp_1 = ((u64)tmp_0 << 3);
                                                                        tmp_2 = (tmp - tmp_1);
                                                                        parsed_hdr->int_md.delay = tmp_2;
                                                                        tmp_3 = delay_tmp_0;
                                                                        u64* value_0;
                                    value_0 = BPF_MAP_LOOKUP_ELEM(ingress_reg_last_arrival, &meta->flow_id);
                                    if (value_0 != NULL) {
                                        __builtin_memcpy(&tmp_4, value_0, sizeof(ingress_reg_last_arrival_value));
                                    } else {
                                                                                tmp_4 = 0;
                                    }

                                                                        tmp_5 = (tmp_3 - tmp_4);
                                                                        parsed_hdr->int_md.jitter = tmp_5;
                                    /* reg_last_arrival_0.write(meta->flow_id, delay_tmp_0) */
                                    int ret = BPF_MAP_UPDATE_ELEM(ingress_reg_last_arrival, &meta->flow_id, &delay_tmp_0, BPF_ANY);
                                    if (ret) {
                                    }
;
                                                                        parsed_hdr->ipv4.diffserv = 32;
                                                                        parsed_hdr->ipv4.totallen = (parsed_hdr->ipv4.totallen + 18);
                                                                        parsed_hdr->udp.length = (parsed_hdr->udp.length + 18);
                                }
                                break;
                            case INGRESS_TBL_FWD1_ACT_INGRESS_FWD_2ND_PASS: 
                                {
                                    parsed_hdr->ethernet.dstMac = value->u.ingress_fwd_2nd_pass.colMAC;
                                                                        meta_2 = ostd;
                                                                        egress_port_2 = value->u.ingress_fwd_2nd_pass.eif;
                                                                        meta_2->drop = false;
                                                                        meta_2->multicast_group = 0;
                                                                        meta_2->egress_port = egress_port_2;
                                                                        ostd = meta_2;
                                }
                                break;
                            default:
                                return TC_ACT_SHOT;
                        }
                    } else {
                        return TC_ACT_SHOT;
;
                    }
                }
;
            }
        }
    }
    {
{
;
            ;
            ;
            ;
        }

        
        if (ostd->clone) {
            do_packet_clones(skb, &clone_session_tbl, ostd->clone_session_id, CLONE_I2E, 1);
        }
        if (ostd->drop) {
            return TC_ACT_SHOT;
        }
        if (ostd->resubmit) {
            compiler_meta__->packet_path = RESUBMIT;
            return TC_ACT_UNSPEC;
        }
        int outHeaderLength = 0;
        if (parsed_hdr->ethernet.ebpf_valid) {
            outHeaderLength += 112;
        }
;        if (parsed_hdr->ipv4.ebpf_valid) {
            outHeaderLength += 160;
        }
;        if (parsed_hdr->udp.ebpf_valid) {
            outHeaderLength += 64;
        }
;        if (parsed_hdr->int_md.ebpf_valid) {
            outHeaderLength += 144;
        }
;
        int outHeaderOffset = BYTES(outHeaderLength) - BYTES(ebpf_packetOffsetInBits);
        if (outHeaderOffset != 0) {
            int returnCode = 0;
            returnCode = bpf_skb_adjust_room(skb, outHeaderOffset, 1, 0);
            if (returnCode) {
                return TC_ACT_SHOT;
            }
        }
        pkt = ((void*)(long)skb->data);
        ebpf_packetEnd = ((void*)(long)skb->data_end);
        ebpf_packetOffsetInBits = 0;
        if (parsed_hdr->ethernet.ebpf_valid) {
            if (ebpf_packetEnd < pkt + BYTES(ebpf_packetOffsetInBits + 112)) {
                return TC_ACT_SHOT;
            }
            
            parsed_hdr->ethernet.dstMac = htonll(parsed_hdr->ethernet.dstMac << 16);
            ebpf_byte = ((char*)(&parsed_hdr->ethernet.dstMac))[0];
            write_byte(pkt, BYTES(ebpf_packetOffsetInBits) + 0, (ebpf_byte));
            ebpf_byte = ((char*)(&parsed_hdr->ethernet.dstMac))[1];
            write_byte(pkt, BYTES(ebpf_packetOffsetInBits) + 1, (ebpf_byte));
            ebpf_byte = ((char*)(&parsed_hdr->ethernet.dstMac))[2];
            write_byte(pkt, BYTES(ebpf_packetOffsetInBits) + 2, (ebpf_byte));
            ebpf_byte = ((char*)(&parsed_hdr->ethernet.dstMac))[3];
            write_byte(pkt, BYTES(ebpf_packetOffsetInBits) + 3, (ebpf_byte));
            ebpf_byte = ((char*)(&parsed_hdr->ethernet.dstMac))[4];
            write_byte(pkt, BYTES(ebpf_packetOffsetInBits) + 4, (ebpf_byte));
            ebpf_byte = ((char*)(&parsed_hdr->ethernet.dstMac))[5];
            write_byte(pkt, BYTES(ebpf_packetOffsetInBits) + 5, (ebpf_byte));
            ebpf_packetOffsetInBits += 48;

            parsed_hdr->ethernet.srcMac = htonll(parsed_hdr->ethernet.srcMac << 16);
            ebpf_byte = ((char*)(&parsed_hdr->ethernet.srcMac))[0];
            write_byte(pkt, BYTES(ebpf_packetOffsetInBits) + 0, (ebpf_byte));
            ebpf_byte = ((char*)(&parsed_hdr->ethernet.srcMac))[1];
            write_byte(pkt, BYTES(ebpf_packetOffsetInBits) + 1, (ebpf_byte));
            ebpf_byte = ((char*)(&parsed_hdr->ethernet.srcMac))[2];
            write_byte(pkt, BYTES(ebpf_packetOffsetInBits) + 2, (ebpf_byte));
            ebpf_byte = ((char*)(&parsed_hdr->ethernet.srcMac))[3];
            write_byte(pkt, BYTES(ebpf_packetOffsetInBits) + 3, (ebpf_byte));
            ebpf_byte = ((char*)(&parsed_hdr->ethernet.srcMac))[4];
            write_byte(pkt, BYTES(ebpf_packetOffsetInBits) + 4, (ebpf_byte));
            ebpf_byte = ((char*)(&parsed_hdr->ethernet.srcMac))[5];
            write_byte(pkt, BYTES(ebpf_packetOffsetInBits) + 5, (ebpf_byte));
            ebpf_packetOffsetInBits += 48;

            parsed_hdr->ethernet.etherType = bpf_htons(parsed_hdr->ethernet.etherType);
            ebpf_byte = ((char*)(&parsed_hdr->ethernet.etherType))[0];
            write_byte(pkt, BYTES(ebpf_packetOffsetInBits) + 0, (ebpf_byte));
            ebpf_byte = ((char*)(&parsed_hdr->ethernet.etherType))[1];
            write_byte(pkt, BYTES(ebpf_packetOffsetInBits) + 1, (ebpf_byte));
            ebpf_packetOffsetInBits += 16;

        }
;        if (parsed_hdr->ipv4.ebpf_valid) {
            if (ebpf_packetEnd < pkt + BYTES(ebpf_packetOffsetInBits + 160)) {
                return TC_ACT_SHOT;
            }
            
            ebpf_byte = ((char*)(&parsed_hdr->ipv4.version))[0];
            write_partial(pkt + BYTES(ebpf_packetOffsetInBits) + 0, 4, 4, (ebpf_byte >> 0));
            ebpf_packetOffsetInBits += 4;

            ebpf_byte = ((char*)(&parsed_hdr->ipv4.ihl))[0];
            write_partial(pkt + BYTES(ebpf_packetOffsetInBits) + 0, 4, 0, (ebpf_byte >> 0));
            ebpf_packetOffsetInBits += 4;

            ebpf_byte = ((char*)(&parsed_hdr->ipv4.diffserv))[0];
            write_byte(pkt, BYTES(ebpf_packetOffsetInBits) + 0, (ebpf_byte));
            ebpf_packetOffsetInBits += 8;

            parsed_hdr->ipv4.totallen = bpf_htons(parsed_hdr->ipv4.totallen);
            ebpf_byte = ((char*)(&parsed_hdr->ipv4.totallen))[0];
            write_byte(pkt, BYTES(ebpf_packetOffsetInBits) + 0, (ebpf_byte));
            ebpf_byte = ((char*)(&parsed_hdr->ipv4.totallen))[1];
            write_byte(pkt, BYTES(ebpf_packetOffsetInBits) + 1, (ebpf_byte));
            ebpf_packetOffsetInBits += 16;

            parsed_hdr->ipv4.identification = bpf_htons(parsed_hdr->ipv4.identification);
            ebpf_byte = ((char*)(&parsed_hdr->ipv4.identification))[0];
            write_byte(pkt, BYTES(ebpf_packetOffsetInBits) + 0, (ebpf_byte));
            ebpf_byte = ((char*)(&parsed_hdr->ipv4.identification))[1];
            write_byte(pkt, BYTES(ebpf_packetOffsetInBits) + 1, (ebpf_byte));
            ebpf_packetOffsetInBits += 16;

            ebpf_byte = ((char*)(&parsed_hdr->ipv4.flags))[0];
            write_partial(pkt + BYTES(ebpf_packetOffsetInBits) + 0, 3, 5, (ebpf_byte >> 0));
            ebpf_packetOffsetInBits += 3;

            parsed_hdr->ipv4.fragOffset = bpf_htons(parsed_hdr->ipv4.fragOffset << 3);
            ebpf_byte = ((char*)(&parsed_hdr->ipv4.fragOffset))[0];
            write_partial(pkt + BYTES(ebpf_packetOffsetInBits) + 0, 5, 0, (ebpf_byte >> 3));
            write_partial(pkt + BYTES(ebpf_packetOffsetInBits) + 0 + 1, 3, 5, (ebpf_byte));
            ebpf_byte = ((char*)(&parsed_hdr->ipv4.fragOffset))[1];
            write_partial(pkt + BYTES(ebpf_packetOffsetInBits) + 1, 5, 0, (ebpf_byte >> 3));
            ebpf_packetOffsetInBits += 13;

            ebpf_byte = ((char*)(&parsed_hdr->ipv4.ttl))[0];
            write_byte(pkt, BYTES(ebpf_packetOffsetInBits) + 0, (ebpf_byte));
            ebpf_packetOffsetInBits += 8;

            ebpf_byte = ((char*)(&parsed_hdr->ipv4.protocol))[0];
            write_byte(pkt, BYTES(ebpf_packetOffsetInBits) + 0, (ebpf_byte));
            ebpf_packetOffsetInBits += 8;

            parsed_hdr->ipv4.hdrChecksum = bpf_htons(parsed_hdr->ipv4.hdrChecksum);
            ebpf_byte = ((char*)(&parsed_hdr->ipv4.hdrChecksum))[0];
            write_byte(pkt, BYTES(ebpf_packetOffsetInBits) + 0, (ebpf_byte));
            ebpf_byte = ((char*)(&parsed_hdr->ipv4.hdrChecksum))[1];
            write_byte(pkt, BYTES(ebpf_packetOffsetInBits) + 1, (ebpf_byte));
            ebpf_packetOffsetInBits += 16;

            parsed_hdr->ipv4.srcIP = htonl(parsed_hdr->ipv4.srcIP);
            ebpf_byte = ((char*)(&parsed_hdr->ipv4.srcIP))[0];
            write_byte(pkt, BYTES(ebpf_packetOffsetInBits) + 0, (ebpf_byte));
            ebpf_byte = ((char*)(&parsed_hdr->ipv4.srcIP))[1];
            write_byte(pkt, BYTES(ebpf_packetOffsetInBits) + 1, (ebpf_byte));
            ebpf_byte = ((char*)(&parsed_hdr->ipv4.srcIP))[2];
            write_byte(pkt, BYTES(ebpf_packetOffsetInBits) + 2, (ebpf_byte));
            ebpf_byte = ((char*)(&parsed_hdr->ipv4.srcIP))[3];
            write_byte(pkt, BYTES(ebpf_packetOffsetInBits) + 3, (ebpf_byte));
            ebpf_packetOffsetInBits += 32;

            parsed_hdr->ipv4.dstIP = htonl(parsed_hdr->ipv4.dstIP);
            ebpf_byte = ((char*)(&parsed_hdr->ipv4.dstIP))[0];
            write_byte(pkt, BYTES(ebpf_packetOffsetInBits) + 0, (ebpf_byte));
            ebpf_byte = ((char*)(&parsed_hdr->ipv4.dstIP))[1];
            write_byte(pkt, BYTES(ebpf_packetOffsetInBits) + 1, (ebpf_byte));
            ebpf_byte = ((char*)(&parsed_hdr->ipv4.dstIP))[2];
            write_byte(pkt, BYTES(ebpf_packetOffsetInBits) + 2, (ebpf_byte));
            ebpf_byte = ((char*)(&parsed_hdr->ipv4.dstIP))[3];
            write_byte(pkt, BYTES(ebpf_packetOffsetInBits) + 3, (ebpf_byte));
            ebpf_packetOffsetInBits += 32;

        }
;        if (parsed_hdr->udp.ebpf_valid) {
            if (ebpf_packetEnd < pkt + BYTES(ebpf_packetOffsetInBits + 64)) {
                return TC_ACT_SHOT;
            }
            
            parsed_hdr->udp.srcPort = bpf_htons(parsed_hdr->udp.srcPort);
            ebpf_byte = ((char*)(&parsed_hdr->udp.srcPort))[0];
            write_byte(pkt, BYTES(ebpf_packetOffsetInBits) + 0, (ebpf_byte));
            ebpf_byte = ((char*)(&parsed_hdr->udp.srcPort))[1];
            write_byte(pkt, BYTES(ebpf_packetOffsetInBits) + 1, (ebpf_byte));
            ebpf_packetOffsetInBits += 16;

            parsed_hdr->udp.dstPort = bpf_htons(parsed_hdr->udp.dstPort);
            ebpf_byte = ((char*)(&parsed_hdr->udp.dstPort))[0];
            write_byte(pkt, BYTES(ebpf_packetOffsetInBits) + 0, (ebpf_byte));
            ebpf_byte = ((char*)(&parsed_hdr->udp.dstPort))[1];
            write_byte(pkt, BYTES(ebpf_packetOffsetInBits) + 1, (ebpf_byte));
            ebpf_packetOffsetInBits += 16;

            parsed_hdr->udp.length = bpf_htons(parsed_hdr->udp.length);
            ebpf_byte = ((char*)(&parsed_hdr->udp.length))[0];
            write_byte(pkt, BYTES(ebpf_packetOffsetInBits) + 0, (ebpf_byte));
            ebpf_byte = ((char*)(&parsed_hdr->udp.length))[1];
            write_byte(pkt, BYTES(ebpf_packetOffsetInBits) + 1, (ebpf_byte));
            ebpf_packetOffsetInBits += 16;

            parsed_hdr->udp.checksum = bpf_htons(parsed_hdr->udp.checksum);
            ebpf_byte = ((char*)(&parsed_hdr->udp.checksum))[0];
            write_byte(pkt, BYTES(ebpf_packetOffsetInBits) + 0, (ebpf_byte));
            ebpf_byte = ((char*)(&parsed_hdr->udp.checksum))[1];
            write_byte(pkt, BYTES(ebpf_packetOffsetInBits) + 1, (ebpf_byte));
            ebpf_packetOffsetInBits += 16;

        }
;        if (parsed_hdr->int_md.ebpf_valid) {
            if (ebpf_packetEnd < pkt + BYTES(ebpf_packetOffsetInBits + 144)) {
                return TC_ACT_SHOT;
            }
            
            parsed_hdr->int_md.flow_id = bpf_htons(parsed_hdr->int_md.flow_id);
            ebpf_byte = ((char*)(&parsed_hdr->int_md.flow_id))[0];
            write_byte(pkt, BYTES(ebpf_packetOffsetInBits) + 0, (ebpf_byte));
            ebpf_byte = ((char*)(&parsed_hdr->int_md.flow_id))[1];
            write_byte(pkt, BYTES(ebpf_packetOffsetInBits) + 1, (ebpf_byte));
            ebpf_packetOffsetInBits += 16;

            parsed_hdr->int_md.delay = htonll(parsed_hdr->int_md.delay);
            ebpf_byte = ((char*)(&parsed_hdr->int_md.delay))[0];
            write_byte(pkt, BYTES(ebpf_packetOffsetInBits) + 0, (ebpf_byte));
            ebpf_byte = ((char*)(&parsed_hdr->int_md.delay))[1];
            write_byte(pkt, BYTES(ebpf_packetOffsetInBits) + 1, (ebpf_byte));
            ebpf_byte = ((char*)(&parsed_hdr->int_md.delay))[2];
            write_byte(pkt, BYTES(ebpf_packetOffsetInBits) + 2, (ebpf_byte));
            ebpf_byte = ((char*)(&parsed_hdr->int_md.delay))[3];
            write_byte(pkt, BYTES(ebpf_packetOffsetInBits) + 3, (ebpf_byte));
            ebpf_byte = ((char*)(&parsed_hdr->int_md.delay))[4];
            write_byte(pkt, BYTES(ebpf_packetOffsetInBits) + 4, (ebpf_byte));
            ebpf_byte = ((char*)(&parsed_hdr->int_md.delay))[5];
            write_byte(pkt, BYTES(ebpf_packetOffsetInBits) + 5, (ebpf_byte));
            ebpf_byte = ((char*)(&parsed_hdr->int_md.delay))[6];
            write_byte(pkt, BYTES(ebpf_packetOffsetInBits) + 6, (ebpf_byte));
            ebpf_byte = ((char*)(&parsed_hdr->int_md.delay))[7];
            write_byte(pkt, BYTES(ebpf_packetOffsetInBits) + 7, (ebpf_byte));
            ebpf_packetOffsetInBits += 64;

            parsed_hdr->int_md.jitter = htonll(parsed_hdr->int_md.jitter);
            ebpf_byte = ((char*)(&parsed_hdr->int_md.jitter))[0];
            write_byte(pkt, BYTES(ebpf_packetOffsetInBits) + 0, (ebpf_byte));
            ebpf_byte = ((char*)(&parsed_hdr->int_md.jitter))[1];
            write_byte(pkt, BYTES(ebpf_packetOffsetInBits) + 1, (ebpf_byte));
            ebpf_byte = ((char*)(&parsed_hdr->int_md.jitter))[2];
            write_byte(pkt, BYTES(ebpf_packetOffsetInBits) + 2, (ebpf_byte));
            ebpf_byte = ((char*)(&parsed_hdr->int_md.jitter))[3];
            write_byte(pkt, BYTES(ebpf_packetOffsetInBits) + 3, (ebpf_byte));
            ebpf_byte = ((char*)(&parsed_hdr->int_md.jitter))[4];
            write_byte(pkt, BYTES(ebpf_packetOffsetInBits) + 4, (ebpf_byte));
            ebpf_byte = ((char*)(&parsed_hdr->int_md.jitter))[5];
            write_byte(pkt, BYTES(ebpf_packetOffsetInBits) + 5, (ebpf_byte));
            ebpf_byte = ((char*)(&parsed_hdr->int_md.jitter))[6];
            write_byte(pkt, BYTES(ebpf_packetOffsetInBits) + 6, (ebpf_byte));
            ebpf_byte = ((char*)(&parsed_hdr->int_md.jitter))[7];
            write_byte(pkt, BYTES(ebpf_packetOffsetInBits) + 7, (ebpf_byte));
            ebpf_packetOffsetInBits += 64;

        }
;
    }
    return -1;
}
SEC("classifier/tc-ingress")
int tc_ingress_func(struct __sk_buff *skb) {
    struct psa_global_metadata *compiler_meta__ = (struct psa_global_metadata *) skb->cb;
    if (compiler_meta__->pass_to_kernel == true) return TC_ACT_OK;
    if (compiler_meta__->packet_path == NORMAL) {
        compiler_meta__->mark = 153;
        struct internal_metadata *md = (struct internal_metadata *)(unsigned long)skb->data_meta;
        if ((void *) ((struct internal_metadata *) md + 1) <= (void *)(long)skb->data) {
            __u16 *ether_type = (__u16 *) ((void *) (long)skb->data + 12);
            if ((void *) ((__u16 *) ether_type + 1) > (void *) (long) skb->data_end) {
                return TC_ACT_SHOT;
            }
            *ether_type = md->pkt_ether_type;
        }
    }
    struct psa_ingress_output_metadata_t ostd = {
            .drop = true,
    };

    struct empty_t resubmit_meta;
    struct hdr_md *hdrMd;
    struct headers *parsed_hdr;
    int ret = -1;
    #pragma clang loop unroll(disable)
    for (int i = 0; i < 4; i++) {
        ostd.resubmit = 0;
        ret = process(skb, (struct headers *) parsed_hdr, &ostd, compiler_meta__, &resubmit_meta);
        if (ostd.drop == 1 || ostd.resubmit == 0) {
            break;
        }
    }
    if (ret != -1) {
        return ret;
    }
    if (ostd.multicast_group != 0) {
        do_packet_clones(skb, &multicast_grp_tbl, ostd.multicast_group, NORMAL_MULTICAST, 2);
        return TC_ACT_SHOT;
    }
    skb->priority = ostd.class_of_service;
    if (!ostd.drop && ostd.egress_port == 0) {
        compiler_meta__->pass_to_kernel = true;
        return bpf_redirect(skb->ifindex, BPF_F_INGRESS);
    }
    return bpf_redirect(ostd.egress_port, 0);
}

SEC("classifier/tc-egress")
int tc_egress_func(SK_BUFF *skb) {
    struct psa_global_metadata *compiler_meta__ = (struct psa_global_metadata *) skb->cb;
    if (compiler_meta__->mark != 153) {
        return TC_ACT_OK;
    }
    unsigned ebpf_packetOffsetInBits = 0;
    unsigned ebpf_packetOffsetInBits_save = 0;
    ParserError_t ebpf_errorCode = NoError;
    void* pkt = ((void*)(long)skb->data);
    void* ebpf_packetEnd = ((void*)(long)skb->data_end);
    u32 ebpf_zero = 0;
    u32 ebpf_one = 1;
    unsigned char ebpf_byte;
    u32 pkt_len = skb->len;
    u64 tstamp = bpf_ktime_get_ns();
    u32 ebpf_input_port = skb->ifindex;
    if (ebpf_input_port == PSA_PORT_RECIRCULATE) {
        ebpf_input_port = P4C_PSA_PORT_RECIRCULATE;
    }
    struct metadata *meta;

    struct hdr_md *hdrMd;
    struct headers *hdr;
    hdrMd = BPF_MAP_LOOKUP_ELEM(hdr_md_cpumap, &ebpf_one);
    if (!hdrMd)
        return TC_ACT_SHOT;
    __builtin_memset(hdrMd, 0, sizeof(struct hdr_md));

    hdr = &(hdrMd->cpumap_hdr);
    meta = &(hdrMd->cpumap_usermeta);
    struct psa_egress_output_metadata_t ostd = {
       .clone = false,
            .drop = false,
        };

    struct psa_egress_input_metadata_t istd = {
            .class_of_service = skb->priority,
            .egress_port = ebpf_input_port,
            .packet_path = compiler_meta__->packet_path,
            .instance = compiler_meta__->instance,
            .parser_error = ebpf_errorCode,
        };
    istd.egress_timestamp = tstamp;
    {
        struct psa_egress_parser_input_metadata_t istd = {
            .egress_port = ebpf_input_port,
            .packet_path = compiler_meta__->packet_path,
        };
        goto start;
        parse_clone: {
/* extract(hdr->ethernet) */
            if (ebpf_packetEnd < pkt + BYTES(ebpf_packetOffsetInBits + 112 + 0)) {
                ebpf_errorCode = PacketTooShort;
                goto reject;
            }

            hdr->ethernet.dstMac = (u64)((load_dword(pkt, BYTES(ebpf_packetOffsetInBits)) >> 16) & EBPF_MASK(u64, 48));
            ebpf_packetOffsetInBits += 48;

            hdr->ethernet.srcMac = (u64)((load_dword(pkt, BYTES(ebpf_packetOffsetInBits)) >> 16) & EBPF_MASK(u64, 48));
            ebpf_packetOffsetInBits += 48;

            hdr->ethernet.etherType = (u16)((load_half(pkt, BYTES(ebpf_packetOffsetInBits))));
            ebpf_packetOffsetInBits += 16;

            hdr->ethernet.ebpf_valid = 1;

;
/* extract(hdr->ipv4) */
            if (ebpf_packetEnd < pkt + BYTES(ebpf_packetOffsetInBits + 160 + 0)) {
                ebpf_errorCode = PacketTooShort;
                goto reject;
            }

            hdr->ipv4.version = (u8)((load_byte(pkt, BYTES(ebpf_packetOffsetInBits)) >> 4) & EBPF_MASK(u8, 4));
            ebpf_packetOffsetInBits += 4;

            hdr->ipv4.ihl = (u8)((load_byte(pkt, BYTES(ebpf_packetOffsetInBits))) & EBPF_MASK(u8, 4));
            ebpf_packetOffsetInBits += 4;

            hdr->ipv4.diffserv = (u8)((load_byte(pkt, BYTES(ebpf_packetOffsetInBits))));
            ebpf_packetOffsetInBits += 8;

            hdr->ipv4.totallen = (u16)((load_half(pkt, BYTES(ebpf_packetOffsetInBits))));
            ebpf_packetOffsetInBits += 16;

            hdr->ipv4.identification = (u16)((load_half(pkt, BYTES(ebpf_packetOffsetInBits))));
            ebpf_packetOffsetInBits += 16;

            hdr->ipv4.flags = (u8)((load_byte(pkt, BYTES(ebpf_packetOffsetInBits)) >> 5) & EBPF_MASK(u8, 3));
            ebpf_packetOffsetInBits += 3;

            hdr->ipv4.fragOffset = (u16)((load_half(pkt, BYTES(ebpf_packetOffsetInBits))) & EBPF_MASK(u16, 13));
            ebpf_packetOffsetInBits += 13;

            hdr->ipv4.ttl = (u8)((load_byte(pkt, BYTES(ebpf_packetOffsetInBits))));
            ebpf_packetOffsetInBits += 8;

            hdr->ipv4.protocol = (u8)((load_byte(pkt, BYTES(ebpf_packetOffsetInBits))));
            ebpf_packetOffsetInBits += 8;

            hdr->ipv4.hdrChecksum = (u16)((load_half(pkt, BYTES(ebpf_packetOffsetInBits))));
            ebpf_packetOffsetInBits += 16;

            hdr->ipv4.srcIP = (u32)((load_word(pkt, BYTES(ebpf_packetOffsetInBits))));
            ebpf_packetOffsetInBits += 32;

            hdr->ipv4.dstIP = (u32)((load_word(pkt, BYTES(ebpf_packetOffsetInBits))));
            ebpf_packetOffsetInBits += 32;

            hdr->ipv4.ebpf_valid = 1;

;
/* extract(hdr->udp) */
            if (ebpf_packetEnd < pkt + BYTES(ebpf_packetOffsetInBits + 64 + 0)) {
                ebpf_errorCode = PacketTooShort;
                goto reject;
            }

            hdr->udp.srcPort = (u16)((load_half(pkt, BYTES(ebpf_packetOffsetInBits))));
            ebpf_packetOffsetInBits += 16;

            hdr->udp.dstPort = (u16)((load_half(pkt, BYTES(ebpf_packetOffsetInBits))));
            ebpf_packetOffsetInBits += 16;

            hdr->udp.length = (u16)((load_half(pkt, BYTES(ebpf_packetOffsetInBits))));
            ebpf_packetOffsetInBits += 16;

            hdr->udp.checksum = (u16)((load_half(pkt, BYTES(ebpf_packetOffsetInBits))));
            ebpf_packetOffsetInBits += 16;

            hdr->udp.ebpf_valid = 1;

;
/* extract(hdr->int_md) */
            if (ebpf_packetEnd < pkt + BYTES(ebpf_packetOffsetInBits + 144 + 0)) {
                ebpf_errorCode = PacketTooShort;
                goto reject;
            }

            hdr->int_md.flow_id = (u16)((load_half(pkt, BYTES(ebpf_packetOffsetInBits))));
            ebpf_packetOffsetInBits += 16;

            hdr->int_md.delay = (u64)((load_dword(pkt, BYTES(ebpf_packetOffsetInBits))));
            ebpf_packetOffsetInBits += 64;

            hdr->int_md.jitter = (u64)((load_dword(pkt, BYTES(ebpf_packetOffsetInBits))));
            ebpf_packetOffsetInBits += 64;

            hdr->int_md.ebpf_valid = 1;

;
             goto accept;
        }
        parse_ethernet: {
/* extract(hdr->ethernet) */
            if (ebpf_packetEnd < pkt + BYTES(ebpf_packetOffsetInBits + 112 + 0)) {
                ebpf_errorCode = PacketTooShort;
                goto reject;
            }

            hdr->ethernet.dstMac = (u64)((load_dword(pkt, BYTES(ebpf_packetOffsetInBits)) >> 16) & EBPF_MASK(u64, 48));
            ebpf_packetOffsetInBits += 48;

            hdr->ethernet.srcMac = (u64)((load_dword(pkt, BYTES(ebpf_packetOffsetInBits)) >> 16) & EBPF_MASK(u64, 48));
            ebpf_packetOffsetInBits += 48;

            hdr->ethernet.etherType = (u16)((load_half(pkt, BYTES(ebpf_packetOffsetInBits))));
            ebpf_packetOffsetInBits += 16;

            hdr->ethernet.ebpf_valid = 1;

;
            u16 select_3;
            select_3 = hdr->ethernet.etherType;
            if (select_3 == 2048)goto parse_ipv4;
            if ((select_3 & 0x0) == (0x0 & 0x0))goto accept;
            else goto reject;
        }
        parse_int: {
/* extract(hdr->int_md) */
            if (ebpf_packetEnd < pkt + BYTES(ebpf_packetOffsetInBits + 144 + 0)) {
                ebpf_errorCode = PacketTooShort;
                goto reject;
            }

            hdr->int_md.flow_id = (u16)((load_half(pkt, BYTES(ebpf_packetOffsetInBits))));
            ebpf_packetOffsetInBits += 16;

            hdr->int_md.delay = (u64)((load_dword(pkt, BYTES(ebpf_packetOffsetInBits))));
            ebpf_packetOffsetInBits += 64;

            hdr->int_md.jitter = (u64)((load_dword(pkt, BYTES(ebpf_packetOffsetInBits))));
            ebpf_packetOffsetInBits += 64;

            hdr->int_md.ebpf_valid = 1;

;
             goto accept;
        }
        parse_ipv4: {
/* extract(hdr->ipv4) */
            if (ebpf_packetEnd < pkt + BYTES(ebpf_packetOffsetInBits + 160 + 0)) {
                ebpf_errorCode = PacketTooShort;
                goto reject;
            }

            hdr->ipv4.version = (u8)((load_byte(pkt, BYTES(ebpf_packetOffsetInBits)) >> 4) & EBPF_MASK(u8, 4));
            ebpf_packetOffsetInBits += 4;

            hdr->ipv4.ihl = (u8)((load_byte(pkt, BYTES(ebpf_packetOffsetInBits))) & EBPF_MASK(u8, 4));
            ebpf_packetOffsetInBits += 4;

            hdr->ipv4.diffserv = (u8)((load_byte(pkt, BYTES(ebpf_packetOffsetInBits))));
            ebpf_packetOffsetInBits += 8;

            hdr->ipv4.totallen = (u16)((load_half(pkt, BYTES(ebpf_packetOffsetInBits))));
            ebpf_packetOffsetInBits += 16;

            hdr->ipv4.identification = (u16)((load_half(pkt, BYTES(ebpf_packetOffsetInBits))));
            ebpf_packetOffsetInBits += 16;

            hdr->ipv4.flags = (u8)((load_byte(pkt, BYTES(ebpf_packetOffsetInBits)) >> 5) & EBPF_MASK(u8, 3));
            ebpf_packetOffsetInBits += 3;

            hdr->ipv4.fragOffset = (u16)((load_half(pkt, BYTES(ebpf_packetOffsetInBits))) & EBPF_MASK(u16, 13));
            ebpf_packetOffsetInBits += 13;

            hdr->ipv4.ttl = (u8)((load_byte(pkt, BYTES(ebpf_packetOffsetInBits))));
            ebpf_packetOffsetInBits += 8;

            hdr->ipv4.protocol = (u8)((load_byte(pkt, BYTES(ebpf_packetOffsetInBits))));
            ebpf_packetOffsetInBits += 8;

            hdr->ipv4.hdrChecksum = (u16)((load_half(pkt, BYTES(ebpf_packetOffsetInBits))));
            ebpf_packetOffsetInBits += 16;

            hdr->ipv4.srcIP = (u32)((load_word(pkt, BYTES(ebpf_packetOffsetInBits))));
            ebpf_packetOffsetInBits += 32;

            hdr->ipv4.dstIP = (u32)((load_word(pkt, BYTES(ebpf_packetOffsetInBits))));
            ebpf_packetOffsetInBits += 32;

            hdr->ipv4.ebpf_valid = 1;

;
            u8 select_4;
            select_4 = hdr->ipv4.protocol;
            if (select_4 == 17)goto parse_udp;
            if ((select_4 & 0x0) == (0x0 & 0x0))goto accept;
            else goto reject;
        }
        parse_udp: {
/* extract(hdr->udp) */
            if (ebpf_packetEnd < pkt + BYTES(ebpf_packetOffsetInBits + 64 + 0)) {
                ebpf_errorCode = PacketTooShort;
                goto reject;
            }

            hdr->udp.srcPort = (u16)((load_half(pkt, BYTES(ebpf_packetOffsetInBits))));
            ebpf_packetOffsetInBits += 16;

            hdr->udp.dstPort = (u16)((load_half(pkt, BYTES(ebpf_packetOffsetInBits))));
            ebpf_packetOffsetInBits += 16;

            hdr->udp.length = (u16)((load_half(pkt, BYTES(ebpf_packetOffsetInBits))));
            ebpf_packetOffsetInBits += 16;

            hdr->udp.checksum = (u16)((load_half(pkt, BYTES(ebpf_packetOffsetInBits))));
            ebpf_packetOffsetInBits += 16;

            hdr->udp.ebpf_valid = 1;

;
            u8 select_5;
            select_5 = hdr->ipv4.diffserv;
            if (select_5 == 32)goto parse_int;
            if ((select_5 & 0x0) == (0x0 & 0x0))goto accept;
            else goto reject;
        }
        start: {
            u32 select_6;
            select_6 = istd.packet_path;
            if (select_6 == 4)goto parse_clone;
            if (select_6 == 0)goto parse_ethernet;
            if ((select_6 & 0x0) == (0x0 & 0x0))goto accept;
            else goto reject;
        }

        reject: {
            if (ebpf_errorCode == 0) {
                return TC_ACT_SHOT;
            }
            goto accept;
        }

    }
    accept:
    istd.parser_error = ebpf_errorCode;

    {

        u8 hit_0;
        {
if (istd.packet_path == 4) {
/* hdr->int_md.setInvalid() */
                hdr->int_md.ebpf_valid = false;
                                hdr->ipv4.totallen = (hdr->ipv4.totallen + 65518);
                                hdr->udp.length = (hdr->udp.length + 65518);
            }
            else {
/* tbl_int_0.apply() */
                {
                    /* construct key */
                    struct egress_tbl_int_key key = {};
                    key.field0 = istd.egress_port;
                    /* value */
                    struct egress_tbl_int_value *value = NULL;
                    /* perform lookup */
                    value = BPF_MAP_LOOKUP_ELEM(egress_tbl_int, &key);
                    if (value == NULL) {
                        /* miss; find default action */
                        hit_0 = 0;
                        value = BPF_MAP_LOOKUP_ELEM(egress_tbl_int_defaultAction, &ebpf_zero);
                    } else {
                        hit_0 = 1;
                    }
                    if (value != NULL) {
                        /* run action */
                        switch (value->action) {
                            case 0: 
                                {
                                }
                                break;
                            case EGRESS_TBL_INT_ACT_EGRESS_INSERT_MD: 
                                {
                                    hdr->int_md.delay = ((u64)istd.egress_timestamp - hdr->int_md.delay);
                                                                        ostd.clone = true;
                                                                        ostd.clone_session_id = 500;
                                }
                                break;
                            case EGRESS_TBL_INT_ACT_EGRESS_JUST_CLONE: 
                                {
                                    ostd.clone = true;
                                                                        ostd.clone_session_id = 500;
                                }
                                break;
                            default:
                                return TC_ACT_SHOT;
                        }
                    } else {
                        return TC_ACT_SHOT;
;
                    }
                }
;            }

        }
    }
    {
        u16 ck_0_state = 0;
{
/* ck_0.clear() */
            ck_0_state = 0;
;
            /* ck_0.add({hdr->ipv4.version, hdr->ipv4.ihl, hdr->ipv4.diffserv, hdr->ipv4.totallen, hdr->ipv4.identification, hdr->ipv4.flags, hdr->ipv4.fragOffset, hdr->ipv4.ttl, hdr->ipv4.protocol, hdr->ipv4.srcIP, hdr->ipv4.dstIP}) */
            {
                u16 ck_0_tmp = 0;
                ck_0_tmp = (hdr->ipv4.version << 12) | (hdr->ipv4.ihl << 8) | hdr->ipv4.diffserv;
                ck_0_state = csum16_add(ck_0_state, ck_0_tmp);
                ck_0_tmp = hdr->ipv4.totallen;
                ck_0_state = csum16_add(ck_0_state, ck_0_tmp);
                ck_0_tmp = hdr->ipv4.identification;
                ck_0_state = csum16_add(ck_0_state, ck_0_tmp);
                ck_0_tmp = (hdr->ipv4.flags << 13) | hdr->ipv4.fragOffset;
                ck_0_state = csum16_add(ck_0_state, ck_0_tmp);
                ck_0_tmp = (hdr->ipv4.ttl << 8) | hdr->ipv4.protocol;
                ck_0_state = csum16_add(ck_0_state, ck_0_tmp);
                ck_0_tmp = (hdr->ipv4.srcIP >> 16);
                ck_0_state = csum16_add(ck_0_state, ck_0_tmp);
                ck_0_tmp = hdr->ipv4.srcIP;
                ck_0_state = csum16_add(ck_0_state, ck_0_tmp);
                ck_0_tmp = (hdr->ipv4.dstIP >> 16);
                ck_0_state = csum16_add(ck_0_state, ck_0_tmp);
                ck_0_tmp = hdr->ipv4.dstIP;
                ck_0_state = csum16_add(ck_0_state, ck_0_tmp);
            }
;
                        hdr->ipv4.hdrChecksum = /* ck_0.get() */
((u16) (~ck_0_state));
            if (/* hdr->int_md.isValid() */
            hdr->int_md.ebpf_valid) {
/* ck_0.clear() */
                ck_0_state = 0;
;
                /* ck_0.subtract({hdr->udp.checksum}) */
                {
                    u16 ck_0_tmp_0 = 0;
                    ck_0_tmp_0 = hdr->udp.checksum;
                    ck_0_state = csum16_sub(ck_0_state, ck_0_tmp_0);
                }
;
                /* ck_0.add({hdr->int_md.flow_id, hdr->int_md.delay, hdr->int_md.jitter}) */
                {
                    u16 ck_0_tmp_1 = 0;
                    ck_0_tmp_1 = hdr->int_md.flow_id;
                    ck_0_state = csum16_add(ck_0_state, ck_0_tmp_1);
                    ck_0_tmp_1 = (hdr->int_md.delay >> 48);
                    ck_0_state = csum16_add(ck_0_state, ck_0_tmp_1);
                    ck_0_tmp_1 = (hdr->int_md.delay >> 32);
                    ck_0_state = csum16_add(ck_0_state, ck_0_tmp_1);
                    ck_0_tmp_1 = (hdr->int_md.delay >> 16);
                    ck_0_state = csum16_add(ck_0_state, ck_0_tmp_1);
                    ck_0_tmp_1 = hdr->int_md.delay;
                    ck_0_state = csum16_add(ck_0_state, ck_0_tmp_1);
                    ck_0_tmp_1 = (hdr->int_md.jitter >> 48);
                    ck_0_state = csum16_add(ck_0_state, ck_0_tmp_1);
                    ck_0_tmp_1 = (hdr->int_md.jitter >> 32);
                    ck_0_state = csum16_add(ck_0_state, ck_0_tmp_1);
                    ck_0_tmp_1 = (hdr->int_md.jitter >> 16);
                    ck_0_state = csum16_add(ck_0_state, ck_0_tmp_1);
                    ck_0_tmp_1 = hdr->int_md.jitter;
                    ck_0_state = csum16_add(ck_0_state, ck_0_tmp_1);
                }
;
                                hdr->udp.checksum = /* ck_0.get() */
((u16) (~ck_0_state));
                                hdr->udp.checksum = (hdr->udp.checksum + 65500);
            }
            ;
            ;
            ;
            ;
        }

        int outHeaderLength = 0;
;;        if (hdr->ethernet.ebpf_valid) {
            outHeaderLength += 112;
        }
;        if (hdr->ipv4.ebpf_valid) {
            outHeaderLength += 160;
        }
;        if (hdr->udp.ebpf_valid) {
            outHeaderLength += 64;
        }
;        if (hdr->int_md.ebpf_valid) {
            outHeaderLength += 144;
        }
;
        int outHeaderOffset = BYTES(outHeaderLength) - BYTES(ebpf_packetOffsetInBits);
        if (outHeaderOffset != 0) {
            int returnCode = 0;
            returnCode = bpf_skb_adjust_room(skb, outHeaderOffset, 1, 0);
            if (returnCode) {
                return TC_ACT_SHOT;
            }
        }
        pkt = ((void*)(long)skb->data);
        ebpf_packetEnd = ((void*)(long)skb->data_end);
        ebpf_packetOffsetInBits = 0;
;;        if (hdr->ethernet.ebpf_valid) {
            if (ebpf_packetEnd < pkt + BYTES(ebpf_packetOffsetInBits + 112)) {
                return TC_ACT_SHOT;
            }
            
            hdr->ethernet.dstMac = htonll(hdr->ethernet.dstMac << 16);
            ebpf_byte = ((char*)(&hdr->ethernet.dstMac))[0];
            write_byte(pkt, BYTES(ebpf_packetOffsetInBits) + 0, (ebpf_byte));
            ebpf_byte = ((char*)(&hdr->ethernet.dstMac))[1];
            write_byte(pkt, BYTES(ebpf_packetOffsetInBits) + 1, (ebpf_byte));
            ebpf_byte = ((char*)(&hdr->ethernet.dstMac))[2];
            write_byte(pkt, BYTES(ebpf_packetOffsetInBits) + 2, (ebpf_byte));
            ebpf_byte = ((char*)(&hdr->ethernet.dstMac))[3];
            write_byte(pkt, BYTES(ebpf_packetOffsetInBits) + 3, (ebpf_byte));
            ebpf_byte = ((char*)(&hdr->ethernet.dstMac))[4];
            write_byte(pkt, BYTES(ebpf_packetOffsetInBits) + 4, (ebpf_byte));
            ebpf_byte = ((char*)(&hdr->ethernet.dstMac))[5];
            write_byte(pkt, BYTES(ebpf_packetOffsetInBits) + 5, (ebpf_byte));
            ebpf_packetOffsetInBits += 48;

            hdr->ethernet.srcMac = htonll(hdr->ethernet.srcMac << 16);
            ebpf_byte = ((char*)(&hdr->ethernet.srcMac))[0];
            write_byte(pkt, BYTES(ebpf_packetOffsetInBits) + 0, (ebpf_byte));
            ebpf_byte = ((char*)(&hdr->ethernet.srcMac))[1];
            write_byte(pkt, BYTES(ebpf_packetOffsetInBits) + 1, (ebpf_byte));
            ebpf_byte = ((char*)(&hdr->ethernet.srcMac))[2];
            write_byte(pkt, BYTES(ebpf_packetOffsetInBits) + 2, (ebpf_byte));
            ebpf_byte = ((char*)(&hdr->ethernet.srcMac))[3];
            write_byte(pkt, BYTES(ebpf_packetOffsetInBits) + 3, (ebpf_byte));
            ebpf_byte = ((char*)(&hdr->ethernet.srcMac))[4];
            write_byte(pkt, BYTES(ebpf_packetOffsetInBits) + 4, (ebpf_byte));
            ebpf_byte = ((char*)(&hdr->ethernet.srcMac))[5];
            write_byte(pkt, BYTES(ebpf_packetOffsetInBits) + 5, (ebpf_byte));
            ebpf_packetOffsetInBits += 48;

            hdr->ethernet.etherType = bpf_htons(hdr->ethernet.etherType);
            ebpf_byte = ((char*)(&hdr->ethernet.etherType))[0];
            write_byte(pkt, BYTES(ebpf_packetOffsetInBits) + 0, (ebpf_byte));
            ebpf_byte = ((char*)(&hdr->ethernet.etherType))[1];
            write_byte(pkt, BYTES(ebpf_packetOffsetInBits) + 1, (ebpf_byte));
            ebpf_packetOffsetInBits += 16;

        }
;        if (hdr->ipv4.ebpf_valid) {
            if (ebpf_packetEnd < pkt + BYTES(ebpf_packetOffsetInBits + 160)) {
                return TC_ACT_SHOT;
            }
            
            ebpf_byte = ((char*)(&hdr->ipv4.version))[0];
            write_partial(pkt + BYTES(ebpf_packetOffsetInBits) + 0, 4, 4, (ebpf_byte >> 0));
            ebpf_packetOffsetInBits += 4;

            ebpf_byte = ((char*)(&hdr->ipv4.ihl))[0];
            write_partial(pkt + BYTES(ebpf_packetOffsetInBits) + 0, 4, 0, (ebpf_byte >> 0));
            ebpf_packetOffsetInBits += 4;

            ebpf_byte = ((char*)(&hdr->ipv4.diffserv))[0];
            write_byte(pkt, BYTES(ebpf_packetOffsetInBits) + 0, (ebpf_byte));
            ebpf_packetOffsetInBits += 8;

            hdr->ipv4.totallen = bpf_htons(hdr->ipv4.totallen);
            ebpf_byte = ((char*)(&hdr->ipv4.totallen))[0];
            write_byte(pkt, BYTES(ebpf_packetOffsetInBits) + 0, (ebpf_byte));
            ebpf_byte = ((char*)(&hdr->ipv4.totallen))[1];
            write_byte(pkt, BYTES(ebpf_packetOffsetInBits) + 1, (ebpf_byte));
            ebpf_packetOffsetInBits += 16;

            hdr->ipv4.identification = bpf_htons(hdr->ipv4.identification);
            ebpf_byte = ((char*)(&hdr->ipv4.identification))[0];
            write_byte(pkt, BYTES(ebpf_packetOffsetInBits) + 0, (ebpf_byte));
            ebpf_byte = ((char*)(&hdr->ipv4.identification))[1];
            write_byte(pkt, BYTES(ebpf_packetOffsetInBits) + 1, (ebpf_byte));
            ebpf_packetOffsetInBits += 16;

            ebpf_byte = ((char*)(&hdr->ipv4.flags))[0];
            write_partial(pkt + BYTES(ebpf_packetOffsetInBits) + 0, 3, 5, (ebpf_byte >> 0));
            ebpf_packetOffsetInBits += 3;

            hdr->ipv4.fragOffset = bpf_htons(hdr->ipv4.fragOffset << 3);
            ebpf_byte = ((char*)(&hdr->ipv4.fragOffset))[0];
            write_partial(pkt + BYTES(ebpf_packetOffsetInBits) + 0, 5, 0, (ebpf_byte >> 3));
            write_partial(pkt + BYTES(ebpf_packetOffsetInBits) + 0 + 1, 3, 5, (ebpf_byte));
            ebpf_byte = ((char*)(&hdr->ipv4.fragOffset))[1];
            write_partial(pkt + BYTES(ebpf_packetOffsetInBits) + 1, 5, 0, (ebpf_byte >> 3));
            ebpf_packetOffsetInBits += 13;

            ebpf_byte = ((char*)(&hdr->ipv4.ttl))[0];
            write_byte(pkt, BYTES(ebpf_packetOffsetInBits) + 0, (ebpf_byte));
            ebpf_packetOffsetInBits += 8;

            ebpf_byte = ((char*)(&hdr->ipv4.protocol))[0];
            write_byte(pkt, BYTES(ebpf_packetOffsetInBits) + 0, (ebpf_byte));
            ebpf_packetOffsetInBits += 8;

            hdr->ipv4.hdrChecksum = bpf_htons(hdr->ipv4.hdrChecksum);
            ebpf_byte = ((char*)(&hdr->ipv4.hdrChecksum))[0];
            write_byte(pkt, BYTES(ebpf_packetOffsetInBits) + 0, (ebpf_byte));
            ebpf_byte = ((char*)(&hdr->ipv4.hdrChecksum))[1];
            write_byte(pkt, BYTES(ebpf_packetOffsetInBits) + 1, (ebpf_byte));
            ebpf_packetOffsetInBits += 16;

            hdr->ipv4.srcIP = htonl(hdr->ipv4.srcIP);
            ebpf_byte = ((char*)(&hdr->ipv4.srcIP))[0];
            write_byte(pkt, BYTES(ebpf_packetOffsetInBits) + 0, (ebpf_byte));
            ebpf_byte = ((char*)(&hdr->ipv4.srcIP))[1];
            write_byte(pkt, BYTES(ebpf_packetOffsetInBits) + 1, (ebpf_byte));
            ebpf_byte = ((char*)(&hdr->ipv4.srcIP))[2];
            write_byte(pkt, BYTES(ebpf_packetOffsetInBits) + 2, (ebpf_byte));
            ebpf_byte = ((char*)(&hdr->ipv4.srcIP))[3];
            write_byte(pkt, BYTES(ebpf_packetOffsetInBits) + 3, (ebpf_byte));
            ebpf_packetOffsetInBits += 32;

            hdr->ipv4.dstIP = htonl(hdr->ipv4.dstIP);
            ebpf_byte = ((char*)(&hdr->ipv4.dstIP))[0];
            write_byte(pkt, BYTES(ebpf_packetOffsetInBits) + 0, (ebpf_byte));
            ebpf_byte = ((char*)(&hdr->ipv4.dstIP))[1];
            write_byte(pkt, BYTES(ebpf_packetOffsetInBits) + 1, (ebpf_byte));
            ebpf_byte = ((char*)(&hdr->ipv4.dstIP))[2];
            write_byte(pkt, BYTES(ebpf_packetOffsetInBits) + 2, (ebpf_byte));
            ebpf_byte = ((char*)(&hdr->ipv4.dstIP))[3];
            write_byte(pkt, BYTES(ebpf_packetOffsetInBits) + 3, (ebpf_byte));
            ebpf_packetOffsetInBits += 32;

        }
;        if (hdr->udp.ebpf_valid) {
            if (ebpf_packetEnd < pkt + BYTES(ebpf_packetOffsetInBits + 64)) {
                return TC_ACT_SHOT;
            }
            
            hdr->udp.srcPort = bpf_htons(hdr->udp.srcPort);
            ebpf_byte = ((char*)(&hdr->udp.srcPort))[0];
            write_byte(pkt, BYTES(ebpf_packetOffsetInBits) + 0, (ebpf_byte));
            ebpf_byte = ((char*)(&hdr->udp.srcPort))[1];
            write_byte(pkt, BYTES(ebpf_packetOffsetInBits) + 1, (ebpf_byte));
            ebpf_packetOffsetInBits += 16;

            hdr->udp.dstPort = bpf_htons(hdr->udp.dstPort);
            ebpf_byte = ((char*)(&hdr->udp.dstPort))[0];
            write_byte(pkt, BYTES(ebpf_packetOffsetInBits) + 0, (ebpf_byte));
            ebpf_byte = ((char*)(&hdr->udp.dstPort))[1];
            write_byte(pkt, BYTES(ebpf_packetOffsetInBits) + 1, (ebpf_byte));
            ebpf_packetOffsetInBits += 16;

            hdr->udp.length = bpf_htons(hdr->udp.length);
            ebpf_byte = ((char*)(&hdr->udp.length))[0];
            write_byte(pkt, BYTES(ebpf_packetOffsetInBits) + 0, (ebpf_byte));
            ebpf_byte = ((char*)(&hdr->udp.length))[1];
            write_byte(pkt, BYTES(ebpf_packetOffsetInBits) + 1, (ebpf_byte));
            ebpf_packetOffsetInBits += 16;

            hdr->udp.checksum = bpf_htons(hdr->udp.checksum);
            ebpf_byte = ((char*)(&hdr->udp.checksum))[0];
            write_byte(pkt, BYTES(ebpf_packetOffsetInBits) + 0, (ebpf_byte));
            ebpf_byte = ((char*)(&hdr->udp.checksum))[1];
            write_byte(pkt, BYTES(ebpf_packetOffsetInBits) + 1, (ebpf_byte));
            ebpf_packetOffsetInBits += 16;

        }
;        if (hdr->int_md.ebpf_valid) {
            if (ebpf_packetEnd < pkt + BYTES(ebpf_packetOffsetInBits + 144)) {
                return TC_ACT_SHOT;
            }
            
            hdr->int_md.flow_id = bpf_htons(hdr->int_md.flow_id);
            ebpf_byte = ((char*)(&hdr->int_md.flow_id))[0];
            write_byte(pkt, BYTES(ebpf_packetOffsetInBits) + 0, (ebpf_byte));
            ebpf_byte = ((char*)(&hdr->int_md.flow_id))[1];
            write_byte(pkt, BYTES(ebpf_packetOffsetInBits) + 1, (ebpf_byte));
            ebpf_packetOffsetInBits += 16;

            hdr->int_md.delay = htonll(hdr->int_md.delay);
            ebpf_byte = ((char*)(&hdr->int_md.delay))[0];
            write_byte(pkt, BYTES(ebpf_packetOffsetInBits) + 0, (ebpf_byte));
            ebpf_byte = ((char*)(&hdr->int_md.delay))[1];
            write_byte(pkt, BYTES(ebpf_packetOffsetInBits) + 1, (ebpf_byte));
            ebpf_byte = ((char*)(&hdr->int_md.delay))[2];
            write_byte(pkt, BYTES(ebpf_packetOffsetInBits) + 2, (ebpf_byte));
            ebpf_byte = ((char*)(&hdr->int_md.delay))[3];
            write_byte(pkt, BYTES(ebpf_packetOffsetInBits) + 3, (ebpf_byte));
            ebpf_byte = ((char*)(&hdr->int_md.delay))[4];
            write_byte(pkt, BYTES(ebpf_packetOffsetInBits) + 4, (ebpf_byte));
            ebpf_byte = ((char*)(&hdr->int_md.delay))[5];
            write_byte(pkt, BYTES(ebpf_packetOffsetInBits) + 5, (ebpf_byte));
            ebpf_byte = ((char*)(&hdr->int_md.delay))[6];
            write_byte(pkt, BYTES(ebpf_packetOffsetInBits) + 6, (ebpf_byte));
            ebpf_byte = ((char*)(&hdr->int_md.delay))[7];
            write_byte(pkt, BYTES(ebpf_packetOffsetInBits) + 7, (ebpf_byte));
            ebpf_packetOffsetInBits += 64;

            hdr->int_md.jitter = htonll(hdr->int_md.jitter);
            ebpf_byte = ((char*)(&hdr->int_md.jitter))[0];
            write_byte(pkt, BYTES(ebpf_packetOffsetInBits) + 0, (ebpf_byte));
            ebpf_byte = ((char*)(&hdr->int_md.jitter))[1];
            write_byte(pkt, BYTES(ebpf_packetOffsetInBits) + 1, (ebpf_byte));
            ebpf_byte = ((char*)(&hdr->int_md.jitter))[2];
            write_byte(pkt, BYTES(ebpf_packetOffsetInBits) + 2, (ebpf_byte));
            ebpf_byte = ((char*)(&hdr->int_md.jitter))[3];
            write_byte(pkt, BYTES(ebpf_packetOffsetInBits) + 3, (ebpf_byte));
            ebpf_byte = ((char*)(&hdr->int_md.jitter))[4];
            write_byte(pkt, BYTES(ebpf_packetOffsetInBits) + 4, (ebpf_byte));
            ebpf_byte = ((char*)(&hdr->int_md.jitter))[5];
            write_byte(pkt, BYTES(ebpf_packetOffsetInBits) + 5, (ebpf_byte));
            ebpf_byte = ((char*)(&hdr->int_md.jitter))[6];
            write_byte(pkt, BYTES(ebpf_packetOffsetInBits) + 6, (ebpf_byte));
            ebpf_byte = ((char*)(&hdr->int_md.jitter))[7];
            write_byte(pkt, BYTES(ebpf_packetOffsetInBits) + 7, (ebpf_byte));
            ebpf_packetOffsetInBits += 64;

        }
;
    }
    if (ostd.clone) {
        do_packet_clones(skb, &clone_session_tbl, ostd.clone_session_id, CLONE_E2E, 3);
    }

    if (ostd.drop) {
        return TC_ACT_SHOT;;
    }

    if (istd.egress_port == P4C_PSA_PORT_RECIRCULATE) {
        compiler_meta__->packet_path = RECIRCULATE;
        return bpf_redirect(PSA_PORT_RECIRCULATE, BPF_F_INGRESS);
    }

    
    return TC_ACT_OK;
}
char _license[] SEC("license") = "GPL";
